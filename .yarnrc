--install.no-lockfile false
--install.frozen-lockfile false

# Ensure we always save exact dependency versions
save-exact true

# Disable package-lock.json generation
package-lock-only false

# Prefer yarn commands
prefer-offline true
